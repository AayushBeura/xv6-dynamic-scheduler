# xv6-dynamic-scheduler
This repository contains an implementation of a custom process scheduler for XV6 that replaces the default round-robin scheduler with one that computes time quanta dynamically. 
